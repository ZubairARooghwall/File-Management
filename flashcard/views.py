from django.shortcuts import render



#Import all the models
from .models import User # Continue adding the models
#Import all the forms
from .forms import  * # Continue adding the models

# Create your views here.
